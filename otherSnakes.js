const { keys } = require("./grid");

function smallSnakesNear(grid, data) {
  //todo
}
function largeSnakeAround(grid, data) {
  //todo
}
function avoidLargeSnakes(grid, data) {
  //todo
}
function hunt(grid, data) {
  //todo
}

module.exports = {
  smallSnakesNear,
  largeSnakeAround,
  avoidLargeSnakes,
  hunt,
};
